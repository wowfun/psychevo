# Psychevo App Server binary

`psychevo-app-server-bin` is the platform wheel used by the `psychevo` Python
SDK. It contains the exact App Server executable for the package version.

Install `psychevo` instead of depending on this wheel directly. This package is
wheel-only and does not search for or download another executable.
