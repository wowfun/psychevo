# Psychevo CLI binary

`psychevo-cli-bin` is the platform wheel for `pevo`. It contains the CLI, its
TUI, and the matching Workbench assets.

Install it through the SDK extra:

```bash
python -m pip install "psychevo[cli]"
```

This package is wheel-only.
