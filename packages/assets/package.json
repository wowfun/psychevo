{
  "name": "@psychevo/assets",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./theme.css": "./theme.css"
  },
  "scripts": {
    "build": "pnpm run design:generate && tsc -p tsconfig.json",
    "check": "pnpm run design:check && tsc -p tsconfig.json",
    "design:check": "node scripts/design-system.mjs check",
    "design:generate": "node scripts/design-system.mjs generate",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "yaml": "2.9.0"
  }
}
