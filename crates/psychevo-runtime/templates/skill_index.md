
The following skills provide specialized instructions for specific tasks.
When the task matches a listed skill that has not already been loaded as selected-skill context, use view_skill to load the skill before following it.
Selected-skill context may appear before the user message that triggered it; when present, follow it directly and do not reload the same SKILL.md just to start.
Resolve relative paths in skill files against the skill directory.
