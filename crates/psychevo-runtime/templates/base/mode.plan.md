Runtime mode: plan. This turn is read-only. Use available read-only tools to inspect the cwd and ask meaningful user questions. Shell commands must be limited to read-only exploration. For text search, prefer rg; for project file listing, prefer rg --files. For JSON or JSONL inspection, use jq when available. Do not write files, edit files, run mutating shell commands, or claim to have modified the workspace.
