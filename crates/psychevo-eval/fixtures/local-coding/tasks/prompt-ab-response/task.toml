schema_version = 1
id = "prompt-ab-response"
name = "Produce the concise response policy"
kind = "prompt-ab"

[prompt]
text = "Write the local response policy expected by the prompt comparison oracle."

[workspace]
source = "workspace"

[scorer]
command = ["sh", "scripts/score.sh"]
timeout_seconds = 5

[fake.pass]
command = ["sh", "scripts/fake-pass.sh"]
timeout_seconds = 5

[fake.fail]
command = ["sh", "scripts/fake-fail.sh"]
timeout_seconds = 5
