schema_version = 1
id = "prompt-ab"
name = "Local prompt A/B fixture"
description = "Tiny prompt-policy comparison fixture with a stable local oracle."
agents = ["fake-pass", "fake-fail"]
tasks = ["../tasks/prompt-ab-response/task.toml"]
